# Development defaults
# By default the app will call /api/openai/chat/completions which assumes a local dev proxy is configured.

## Remote Azure Function proxy configuration
# When set, the app will bypass local proxying and call the Azure Function directly.
# Full proxy endpoint (optional – if omitted the app will derive it from REACT_APP_FUNCTION_BASE_URL):
REACT_APP_OPENAI_PROXY_URL=https://mortongroupaicred-hugxh8drhqabbphb.canadacentral-01.azurewebsites.net/api/openai/chat/completions

# Base Function URL used for general API proxying (/api/*) in development & for absolute URL construction in production.
REACT_APP_FUNCTION_BASE_URL=https://mortongroupaicred-hugxh8drhqabbphb.canadacentral-01.azurewebsites.net

# Optional legacy direct config (not used when proxy is set)
# REACT_APP_AZURE_OPENAI_ENDPOINT=YOUR_AZURE_OPENAI_ENDPOINT
# REACT_APP_AZURE_OPENAI_API_KEY=YOUR_AZURE_OPENAI_KEY

# Azure OpenAI Configuration
REACT_APP_AZURE_OPENAI_ENDPOINT=YOUR_AZURE_OPENAI_ENDPOINT
REACT_APP_AZURE_OPENAI_API_KEY=YOUR_AZURE_OPENAI_API_KEY
REACT_APP_AZURE_OPENAI_DEPLOYMENT=gpt-4o
REACT_APP_AZURE_OPENAI_API_VERSION=2024-02-15-preview

# DEPRECATED: Azure AD Configuration for Microsoft Sign-In
# Azure Static Web Apps now handles authentication automatically via /.auth endpoints
# The following variables are no longer required but kept for reference:
# REACT_APP_AZURE_AD_CLIENT_ID=YOUR_AZURE_AD_CLIENT_ID
# REACT_APP_AZURE_AD_AUTHORITY=https://login.microsoftonline.com/common
# REACT_APP_REDIRECT_URI=http://localhost:3000

# Authentication Mode
# Set to false to enable Microsoft Sign-In authentication in production
# Set to true for development mode (bypasses authentication)
REACT_APP_SKIP_AUTH=true

# Application Settings
REACT_APP_NAME=Mo Money
REACT_APP_VERSION=1.0.0
